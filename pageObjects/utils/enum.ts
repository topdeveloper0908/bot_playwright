/**
 * timeout durations
 */
export enum Timeout {
    SHORT = 2000,
    MEDIUM = 15000,
    LONG = 30000,
}

/**
 * Urls
 */
export enum Urls {
    baseUrl = "https://casper.com/",
    allProducts = '/mattresses',
    product = '/original',
    cart = '/cart',
    checkout = '/checkout'
}